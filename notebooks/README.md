# Notebooks

Notebook chỉ dùng cho exploration và visualization.

- Không dùng làm bước duy nhất để tạo split, metric hoặc artifact báo cáo.
- Logic ổn định phải chuyển vào `ml/` hoặc `scripts/`.
- Notebook ghi data version và run ID ở đầu.

