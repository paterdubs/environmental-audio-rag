# Driver cho PostgreSQL + pgvector (ADR-0005, H3). Tách riêng vì `services/api`
# và W6 mới cần — cổng dữ liệu và training không đụng tới.
#
#   .venv/Scripts/python.exe -m pip install -r requirements-db.txt

psycopg[binary]>=3.2,<4
pgvector>=0.3,<1
alembic>=1.13,<2
